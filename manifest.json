{
  "manifest_version": 3,
  "name": "Domain-Specific Command Extension",
  "version": "1.0",
  "description": "This extension enables a command only on specific domains.",
  "permissions": ["activeTab", "scripting"],
  "background": {
    "service_worker": "background.js"
  },
  "commands": {
    "direct-to-maps": {
      "suggested_key": {
        "default": "Ctrl+Shift+Y"
      },
      "description": "Run command only on specified domains"
    }
  }
}
